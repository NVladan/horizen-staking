# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node (OpenZeppelin vendored for compilation)
node_modules/

# Build artifacts (compiled ABIs/bytecode) are committed so deploys need no
# Solidity toolchain. Recompile with `python -m scripts.compile` after any
# contract change.

# Secrets & local config
.env
.claude/settings.local.json

# Runtime logs
logs/

# OS
Thumbs.db
.DS_Store
