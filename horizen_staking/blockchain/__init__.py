"""Blockchain layer: compilation, connection, contract loading, read service.

This subpackage is intentionally free of any Flask dependency so it can be
reused by the ``scripts/`` (compile/deploy/fund) and by ``tests/``.
"""
